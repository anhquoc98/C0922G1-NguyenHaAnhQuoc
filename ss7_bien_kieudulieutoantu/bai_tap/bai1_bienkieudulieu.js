let vatly ;
let hoahoc;
let sinhhoc;
vatly =prompt('vatly');
hoahoc =prompt('hoahoc');
sinhhoc =prompt('sinhhoc');
let x = parseInt(vatly);
let y = parseInt(hoahoc);
let z= parseInt(sinhhoc);
let are;

are = (x + y + z) / 3;
document.write('are ='+are);